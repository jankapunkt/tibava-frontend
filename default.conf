server {
    listen       80;
    server_name  localhost;
    root         /frontend/dist/;
    

    location /thumbnails/ {
        alias /thumbnails/;
    }
    
    location /media/ {
        alias /media/;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
